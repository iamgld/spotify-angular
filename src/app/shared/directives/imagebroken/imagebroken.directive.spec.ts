import { ImagebrokenDirective } from './imagebroken.directive';

describe('ImagebrokenDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagebrokenDirective();
    expect(directive).toBeTruthy();
  });
});
